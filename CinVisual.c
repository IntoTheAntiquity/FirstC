#include <stdio.h>

int main() {
    printf("Hello, World!\n");
    return 0;
}
/* 
 *dont forget to compile the code 
 * needs to be complied after each change made to it.
 * to compile gcc -o CinVisual CinVisual.c
 * scratch that i rememebr having to compile it multiple times on the terminal
 * maybe that was just for the terminal Ctrl+S to save and it saves it completely
 */